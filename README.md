# esp_ws28xx
A light and simple ESP-IDF lib for WS2812B/WS2815 led strips. Works via SPI with DMA. 
A fork of https://github.com/okhsunrog/esp_ws28xx/tree/main. Adds the possibility to have multiple instances of a WS28xx driver at the same time.

# Example of usage

Just create folder `components` in the root of your project and clone this repo to the created folder.

Example code will be provided soon™.
